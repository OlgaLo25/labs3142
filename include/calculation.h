#ifndef CALCULATION_H
#define CALCULATION_H
#include <vector>
using namespace std;
#include "structure.h"
#include "cmath"
#include "utils.h"

class Calculation{
    public:
    double getCalculation(vector<Lexem> resultVector);
}; 
#endif